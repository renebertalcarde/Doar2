/// <reference types="react" />
import { SpaceProps, BorderProps } from "@doar/shared/styled";
interface IProps extends SpaceProps, BorderProps {
}
export declare const StyledHr: import("styled-components").StyledComponent<({ my, mt, mb, py, pt, pb, borderWidth, ...rest }: any) => JSX.Element, any, IProps, never>;
export {};
