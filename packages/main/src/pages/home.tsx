import { Card, CardBody, CardTitle } from "@doar/components";

const Home = () => {
    return (
        <Card>
            <CardBody>
                <CardTitle>Home</CardTitle>
                <p>Welcome to the Home page.</p>
            </CardBody>
        </Card>
    );
};

export default Home;